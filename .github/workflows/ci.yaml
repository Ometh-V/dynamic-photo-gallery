name: Expose Docker Container via ngrok

on:
  push:
    branches:
      - Adding-CI

jobs:
  build-container:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Build Docker image
        run: docker build -t myapp .
      
      - name: Docker login
        uses: docker/login-action@v2
        run: docker login -u ${{ secrets.DOCKERHUB_USERNAME }} -p ${{ secrets.DOCKERHUB_TOKEN }}
      
      - name: Push Docker image
        run: |
          docker push ${{ secrets.DOCKERHUB_USERNAME }}/photo-app:${{ github.sha }}

      # - name: Wait for container to be ready
      #   run: |
      #     for i in {1..10}; do
      #       curl -sf http://localhost:3000 && break
      #       echo "Waiting for app..."
      #       sleep 3
      #     done

      # - name: Show container logs (debugging)
      #   if: always()
      #   run: docker logs myapp-container

      # - name: Install ngrok
      #   run: |
      #     curl -s https://ngrok-agent.s3.amazonaws.com/ngrok.asc | sudo tee /etc/apt/trusted.gpg.d/ngrok.asc >/dev/null
      #     echo "deb https://ngrok-agent.s3.amazonaws.com buster main" | sudo tee /etc/apt/sources.list.d/ngrok.list
      #     sudo apt update && sudo apt install ngrok

      # - name: Authenticate ngrok
      #   run: ngrok config add-authtoken ${{ secrets.NGROK_TOKEN }}

      # - name: Start ngrok tunnel
      #   run: |
      #     ngrok http 3000 --log=stdout > ngrok.log &
      #     sleep 5

      # - name: Extract and show ngrok URL
      #   id: ngrok
      #   run: |
      #     URL=$(curl -s http://localhost:4040/api/tunnels | python3 -c "import sys, json; print(json.load(sys.stdin)['tunnels'][0]['public_url'])")
      #     echo "url=$URL" >> "$GITHUB_OUTPUT"
      #     echo "🔗 Public URL: $URL"

      # - name: Keep tunnel alive
      #   run: sleep 300   # adjust how long you want it exposed